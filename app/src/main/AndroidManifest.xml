<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.heem.togetherfit">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <user-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".StudentLogin"
            android:label="@string/title_activity_login_student" />
        <activity android:name=".StudentRegistration" />
        <activity android:name=".TrainerRegistration" />
        <activity android:name=".StudentRegistrationAdd" />
        <activity android:name=".TrainerLogin" />
        <activity android:name=".TrainerDashboard" />
        <activity android:name=".TrainerRegistrationAdd" />
        <activity android:name=".ChatRoom" />
        <activity android:name=".LiveChat" />
        <activity android:name=".TrainerCreateClass" />
        <activity android:name=".FindTrainer" />
        <activity android:name=".ShowTrainer" />
        <activity android:name=".TrainerCreateClassAdd" />
        <!-- create class -->


        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCBf_lPoUDSxDRJif0Om6shGAKtUhormvo" />

        <activity
            android:name=".FindPlaceTrainer"
            android:label="@string/title_activity_find_place_trainer" />
        <activity
            android:name=".FindPlaceStudent"
            android:label="Map" />
        <activity android:name=".StudentDashboard" />
        <activity android:name=".AddClass" />
        <activity android:name=".ClassDetail" />
        <activity android:name=".ConfirmRegistration" />
        <activity android:name=".ShowClasses" />
        <activity android:name=".EditProfileStudent" />
        <activity android:name=".EditProfileTrainer" />
        <activity android:name=".UserRegInAClass" />
        <activity android:name=".SendEmailThroughAndroid" />
        <activity
            android:name=".showtrainermap"
            android:label="Map" />
        <activity android:name=".EmailSentConfirmation"></activity>
    </application>

</manifest>